spring:
  application:
    name: bookapi
  datasource:
    url: jdbc:h2:mem:book
    username: admin
    password: admin
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console
